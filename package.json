{
  "name": "discord-static-gif",
  "version": "1.0.0",
  "private": true,
  "description": "GIFStar — turn static Discord image attachments into saveable GIFs.",
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "check": "node --check api/interactions.js && node --check scripts/register.js",
    "register": "node scripts/register.js"
  },
  "dependencies": {
    "@vercel/functions": "^3.9.5",
    "sharp": "^0.35.4",
    "tweetnacl": "^1.0.3"
  }
}
